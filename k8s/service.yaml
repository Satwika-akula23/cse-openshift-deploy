apiVersion: v1
kind: Service
metadata:
  name: springboot-openshift-service
spec:
  type: NodePort
  selector:
    app: cseoc-app
  ports:
    - port: 8087
      targetPort: 8087
      nodePort: 30007